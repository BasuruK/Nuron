# Decision Log — Nuron PRD

Canonical memory and audit trail for this PRD run. Every decision, change, and override (including headless overrides) is recorded here as the conversation unfolds.

---

## 2026-07-08 — Workspace bound

- Intent detected: **Create** (no prior PRD found under `_bmad-output/planning-artifacts/prds/`).
- Source input identified: `_bmad-output/planning-artifacts/briefs/brief-Nuron-2026-06-19/brief.md` + `addendum.md` (product brief, status: draft, last updated 2026-07-07).
- No additional inputs beyond the brief/addendum (user confirmed).
- Stakes calibration: **Internal tool** (moderate depth, ~5-8 pages target).
- Working mode: **Coaching path**, entry point **Vision + Features** (capability-first).
- Concern scan (drives Adapt-In Menu): decision-provenance/audit trail (core differentiator, not optional), data governance / tenant isolation / right-to-be-forgotten (resolved in brief, needs FR-level acceptance criteria), operational requirements (curator subtree performance budget, RabbitMQ durability/restart-safety), API contracts (REST + SSE is the actual product contract per brief), integration/dependencies (Neo4j, RabbitMQ, LLM/embedding providers), rollout/phasing (v1 → v1.1 connectors → v2 multi-tenant, already staged in brief).

## 2026-07-08 — Corrections to brief carried into PRD (user-supplied, override brief text)

- **Agentic framework correction:** the brief's narrative names "LangGraph" for the compiler/default agent in several places. User corrects: the agentic backend (structuring agent, compiler, curator, default agent) is built on **LlamaIndex** (workflows), not LangChain/LangGraph. PRD uses "LlamaIndex-based agentic pipeline" language throughout instead of "LangGraph." LlamaIndex `PropertyGraphIndex` over Neo4j (already in the brief) is consistent with this and unchanged. Flagged as a conflict with the brief text — Architecture phase should reconcile the brief's LangGraph references against this correction.
- **Data model correction:** the graph is not decision-nodes-only. Nuron stores **Decision nodes** (first-class, with lineage) **and** related **Entity / Knowledge nodes** — product documentation, code documentation, and tacit business-flow knowledge that domain experts know by heart but rarely write down. Decisions cite/relate to these knowledge nodes; they are graph-first-class in their own right, not merely evidence attachments. Glossary and Vision updated accordingly.
- **Backend confirmation:** `nuron-api` runs on the **latest version of Laravel** (consistent with brief; no change, just confirmed explicitly for the PRD).

## 2026-07-08 — Full draft assembled

- Feature grouping (A. Ingestion & Structuring, B. Compilation, C. Property Graph & Curation, D. Query & Retrieval, E. Agents, F. Admin/Access/Tenant Governance) approved as-is by user.
- FR depth: full testable "Consequences" bullets per FR (user choice).
- API contract detail: high-level only in PRD (endpoints named, no schemas) — full schemas deferred to Architecture (user choice).
- Full draft (§0–§10, FR-1 through FR-17, SM-1 through SM-5, 6 Open Questions) written to `prd.md`.

## 2026-07-08 — Input reconciliation

- Subagent reconciliation vs `brief.md` → `reconcile-brief.md`. Gaps applied: broadened Entity/Knowledge Node scope in Vision to explicitly include operational + people-handling knowledge (brief's domains×flows×people×systems framing); added competitive-differentiation/"moat" rationale tying no-telemetry, write-time supersession, and RabbitMQ durability together in Vision §1.
- Subagent reconciliation vs `addendum.md` → `reconcile-addendum.md`. Gap applied: added parked Apache APISIX edge-gateway candidate to §5.5 Rollout & Phasing (v1.1/v2).
- False positive noted and NOT applied: subagent flagged an "in-process vs RabbitMQ" conflict against addendum §I, but addendum §I is pre-decision rationale explicitly overridden by brief.md's resolved Q-I ("event-driven by design"). PRD FR-4 correctly reflects the resolved state; no change needed.
- Frontend Stack Decision full comparison table (Bits UI vs shadcn-svelte) intentionally left in brief/addendum only — PRD Glossary/§5.3 name the locked choice (Bits UI) without duplicating the rationale table, per PRD Discipline ("tech choices live in addendum.md").
